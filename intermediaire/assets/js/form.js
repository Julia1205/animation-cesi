$( document ).ready(function() {
    // ajouter un input dans la div de class input
    $(".input").html()
    $('.button').html('<input type="button" value="valider">')

    // fonction anonyme lors du clic sur la div de class button
    $('.button').on('click', function(){
        // faire apparaître en console ou sur la page la valeur du champ de saisie
    })
});